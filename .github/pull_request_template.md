## What changed

<!-- Keep the change focused and link its issue. -->

Closes #

## Measurement contract

- Claim this change licenses:
- Evidence it records:
- Conditions that fail closed:
- Explicit non-claims:

Use `Not applicable` with a short explanation for documentation-only changes.

## Verification

- [ ] `python -m unittest discover -s tests -v`
- [ ] `ruff check .`
- [ ] `ruff format --check .`
- [ ] Tests are keyless and make no unplanned network calls.
- [ ] No credential, authorization header, or sensitive judge input is committed.
- [ ] Public API or CLI changes are documented.

